angular.module('SECP')
    .controller('AuthenticateController', function ($scope) {
        $scope.authenticate = function () {
            // TODO: ADD AUTHENTICATION FUNCTION THAT HANDLES WHEN THE USER CLICKS THE VERIFY BUTTON
        }
    });
